You need to place this code inside the setupEventListeners function in your existing JavaScript. Here's exactly where:

Location in your code

Look for the setupEventListeners function in your script. It should be around line 3980-4000 (near the end of the script). Inside that function, you'll see many if statements adding event listeners.

Find this section:

```javascript
setupEventListeners: function() {
    const siteSelector = document.getElementById('siteSelector');
    if (siteSelector) {
        siteSelector.addEventListener('change', (e) => {
            // ... existing code ...
        });
    }

    // ... many other event listeners ...

    // Look for this section near the end of the function:
    document.querySelectorAll('[data-bs-toggle="tab"]').forEach(tab => {
        tab.addEventListener('shown.bs.tab', (e) => {
            // ... EXISTING CODE HERE ...
        });
    });
}
```

Replace the existing document.querySelectorAll('[data-bs-toggle="tab"]') section with the new code.

Complete replacement section:

Find the existing tab event listener code and replace the entire block with:

```javascript
// Tab change event listeners - refresh content when tabs are shown
document.querySelectorAll('[data-bs-toggle="tab"]').forEach(tab => {
    tab.addEventListener('shown.bs.tab', (e) => {
        const target = e.target.getAttribute('data-bs-target');
        
        if (target === '#dashboard') {
            this.loadDashboard();
            // Refresh map when dashboard tab is shown
            setTimeout(() => {
                if (window.map) {
                    window.map.invalidateSize(true);
                } else {
                    this.initMap();
                }
            }, 300);
        } else if (target === '#site-assessment') {
            this.loadAssessments();
        } else if (target === '#maintenance-plan') {
            this.loadActivities();
            if (window.calendar) try { window.calendar.refetchEvents(); } catch (e) { console.error(e); }
        } else if (target === '#species-register') {
            this.loadSpecies();
        } else if (target === '#monitoring') {
            this.loadMonitoring();
        } else if (target === '#reports') {
            this.loadReports();
        } else if (target === '#legislation') {
            this.loadComplianceChecklist();
        } else if (target === '#settings') {
            this.loadSettings();
        }
    });
});
```

Visual guide:

```
setupEventListeners: function() {
    // ... all your existing event listeners ...
    
    // ↓↓↓ FIND THIS SECTION ↓↓↓
    document.querySelectorAll('[data-bs-toggle="tab"]').forEach(tab => {
        tab.addEventListener('shown.bs.tab', (e) => {
            // This is the code you need to REPLACE
        });
    });
    // ↑↑↑ REPLACE THE ABOVE SECTION ↑↑↑
    
    // ... rest of your event listeners ...
}
```

This ensures the map refreshes properly whenever you switch to the Dashboard tab, fixing the display issue.
