I see the problem! You have a duplicate function in your code that's causing issues. Look carefully at your Utils object - you have generateSiteLocation written twice (right next to each other).

🔴 The Problem (Simple Fix!)

In your Utils object, you have this:

```javascript
generateSiteLocation: function(siteId) {
    // ... lots of code ...
},

generateSiteLocation: function(siteId) {  // ← DUPLICATE! Delete this whole second one
    // ... same code again ...
},
```

Having the same function twice confuses JavaScript. The second one overwrites the first, and it might cause other problems.

✅ How To Fix It (30 seconds)

Find this spot in your code (around line 1830-1900). Look for:

```javascript
generateSiteLocation: function(siteId) {
    const baseLat = -33.4000;
    // ... lots of code ...
},
```

You'll see it appears two times in a row.

DELETE the SECOND one completely. Keep only ONE copy of generateSiteLocation.

---

🔍 Also Check - Your Map Might Not Be Initializing

I noticed your map has a more complex issue. The charts and data aren't showing because:

1. The map is trying to load before the page is ready
2. The dashboard tab needs to be active for the map to load

🛠️ Quick Test

Open your browser's Console (F12 key, then click "Console" tab). Do you see any RED error messages? That will tell us exactly what's wrong.

📝 What Should Appear on Dashboard

When working correctly, the dashboard should show:

· 4 colored boxes with numbers (Upcoming Tasks, Assessments, Species, Compliance)
· A radar chart (the spider-web looking graph)
· A list of upcoming tasks
· A map (showing your site)

If you don't see these, the JavaScript isn't running properly.

🚨 Most Likely Issue

Looking at your code, I think the map initialization is failing because it's trying to load when the dashboard tab isn't visible.

Try this simple test:

1. Save your file
2. Refresh the page
3. Click on a different tab (like "Species Register")
4. Then click back on "Dashboard"

Does the map and charts appear now? If yes, that confirms the issue!

---

🎯 The Complete Fix

Replace your entire initMap function with this simpler version (find it near the bottom of your script):

```javascript
initMap: function() {
    console.log('Starting map...');
    
    const mapContainer = document.getElementById('map');
    if (!mapContainer) {
        console.error('No map container');
        return;
    }
    
    const site = Utils.getCurrentSite();
    if (!site) {
        mapContainer.innerHTML = '<div class="alert alert-warning">No site selected</div>';
        return;
    }
    
    // Get map data
    let mapData = db.siteMaps[db.currentSiteId];
    if (!mapData) {
        mapData = Utils.generateSiteLocation(db.currentSiteId);
        db.siteMaps[db.currentSiteId] = mapData;
    }
    
    // Clean up old map
    if (window.map) {
        try { window.map.remove(); } catch(e) {}
        window.map = null;
    }
    
    try {
        // Make sure container is ready
        mapContainer.style.height = '500px';
        mapContainer.innerHTML = '';
        
        // Create map
        window.map = L.map('map').setView(mapData.center, 13);
        
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '&copy; OpenStreetMap'
        }).addTo(window.map);
        
        // Add site marker
        L.marker(mapData.center).bindPopup(`<b>${site.name}</b>`).addTo(window.map);
        
        // Create layers
        this.createMapLayers(mapData);
        
        // Fix size after a moment
        setTimeout(() => {
            if (window.map) window.map.invalidateSize();
        }, 200);
        
        console.log('Map loaded!');
        
    } catch(error) {
        console.error('Map error:', error);
        mapContainer.innerHTML = '<div class="alert alert-danger">Map failed to load</div>';
    }
},
```

---

📋 Summary Checklist

Issue Status Fix
Duplicate generateSiteLocation ❌ Needs fix Delete the second copy
Map not showing ⚠️ Likely Replace initMap function
Charts not showing ⚠️ Likely Will fix when map is fixed

Try the duplicate function fix first (delete the second generateSiteLocation). Then refresh and see if anything changes.

Let me know what you see in the Console (F12 → Console tab) - that will tell us exactly what's wrong!
