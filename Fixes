Looking at your code, I can see the issue - the map tab event handler was placed inside the loadDashboard() function instead of in the setupEventListeners() function. Let me provide you with the corrected version. Here's the fix:

Find this section in your code (around line 3630 in the JavaScript section):

```javascript
// Force map refresh when dashboard tab is shown
const dashboardTab = document.getElementById('dashboard-tab');
if (dashboardTab) {
    dashboardTab.addEventListener('shown.bs.tab', function() {
        if (window.map) {
            setTimeout(() => {
                window.map.invalidateSize(true);
                console.log('Map refreshed on tab show');
            }, 300);
        } else {
            // If map doesn't exist, initialize it
            App.initMap();
        }
    });
}
```

Delete that entire block from where it is (it's currently inside loadDashboard()).

Then, in the setupEventListeners() function (around line 4840), add this code INSIDE the existing function, right after the other tab event listeners:

Look for this section in setupEventListeners():

```javascript
document.querySelectorAll('[data-bs-toggle="tab"]').forEach(tab => {
    tab.addEventListener('shown.bs.tab', (e) => {
        const target = e.target.getAttribute('data-bs-target');
        
        if (target === '#dashboard') {
            this.loadDashboard();
        } else if (target === '#site-assessment') {
            this.loadAssessments();
        } else if (target === '#maintenance-plan') {
            this.loadActivities();
            if (window.calendar) try { window.calendar.refetchEvents(); } catch (e) { console.error(e); }
        } else if (target === '#species-register') {
            this.loadSpecies();
        } else if (target === '#monitoring') {
            this.loadMonitoring();
        } else if (target === '#reports') {
            this.loadReports();
        } else if (target === '#legislation') {
            this.loadComplianceChecklist();
        } else if (target === '#settings') {
            this.loadSettings();
        }

        // Refresh map when dashboard tab is shown
        if (target === '#dashboard' && window.map) {
            setTimeout(() => {
                window.map.invalidateSize(true);
            }, 300);
        }
    });
});
```

Replace that entire block with this corrected version:

```javascript
document.querySelectorAll('[data-bs-toggle="tab"]').forEach(tab => {
    tab.addEventListener('shown.bs.tab', (e) => {
        const target = e.target.getAttribute('data-bs-target');
        
        if (target === '#dashboard') {
            this.loadDashboard();
        } else if (target === '#site-assessment') {
            this.loadAssessments();
        } else if (target === '#maintenance-plan') {
            this.loadActivities();
            if (window.calendar) try { window.calendar.refetchEvents(); } catch (e) { console.error(e); }
        } else if (target === '#species-register') {
            this.loadSpecies();
        } else if (target === '#monitoring') {
            this.loadMonitoring();
        } else if (target === '#reports') {
            this.loadReports();
        } else if (target === '#legislation') {
            this.loadComplianceChecklist();
        } else if (target === '#settings') {
            this.loadSettings();
        }
    });
});

// Force map refresh when dashboard tab is shown - SEPARATE HANDLER
const dashboardTab = document.getElementById('dashboard-tab');
if (dashboardTab) {
    dashboardTab.addEventListener('shown.bs.tab', function() {
        if (window.map) {
            setTimeout(() => {
                window.map.invalidateSize(true);
                console.log('Map refreshed on tab show');
            }, 300);
        } else {
            // If map doesn't exist, initialize it
            App.initMap();
        }
    });
}
```

The key changes are:

1. The map tab handler is now properly placed in setupEventListeners() instead of inside loadDashboard()
2. It's separate from the other tab handlers to ensure it runs properly
3. It checks if the map exists and either refreshes it or initializes it

This should fix your map issue. The map will now properly refresh when you switch to the dashboard tab, and it will initialize properly when first loaded.
